# Publishing: authenticate with a GitHub Personal Access Token (PAT)
# that has write:packages scope.
#
# Generate a PAT at: https://github.com/settings/tokens
# Required scope: write:packages
#
# For CI (GitHub Actions), use GITHUB_TOKEN automatically — no .npmrc needed.
#
# For local publishing:
#   npm login --registry=https://npm.pkg.github.com
#   (enter your GitHub username and PAT when prompted)

@office-of-the-citizen:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
